# ============================================================================
# Configuration d'évaluation YOLOv8
# ============================================================================

dataset_dir: /home/mokira3d48/datasets/numpr
num_classes: 36
weights: checkpoints/best.pt

version: n
image_size: 640
batch_size: 16
num_workers: 4
device: cuda

conf_threshold: 0.001
iou_threshold: 0.7
split: test                # 'test' ou 'train'
