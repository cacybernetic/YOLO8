# ============================================================================
# Configuration d'évaluation YOLOv8
# ============================================================================

dataset_dir: /chemin/vers/dataset_dir
num_classes: 80
weights: checkpoints/best.pt

version: n
image_size: 640
batch_size: 16
num_workers: 4
device: cuda

conf_threshold: 0.001
iou_threshold: 0.7
split: test                # 'test' ou 'train'
